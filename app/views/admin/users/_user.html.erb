
<div>
	<%= user.id %>
	<%= user.email %>
	<%= user.username %>
	<%= user.password %>
 
  <%= link_to t('.edit'), edit_admin_user_path(user) %>
    
  <%= link_to t('.delete'), admin_user_path(user), data: {
  turbo_method: :delete,
  turbo_confirm: t('common.confirm')
} %>

Ver datos de este usuario
</div>
