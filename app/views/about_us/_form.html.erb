<%= form_with(model: about_u) do |form| %>
  <% if about_u.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(about_u.errors.count, "error") %> prohibited this about_u from being saved:</h2>

      <ul>
        <% about_u.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.text_field :title, placeholder: t('.title')  %>
  </div>

  <div>
    <%= form.rich_text_area :body %>
  </div>

  <div>
    <%= form.submit t('common.accept') %>
  </div>
<% end %>
