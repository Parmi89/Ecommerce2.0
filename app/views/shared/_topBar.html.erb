<div>
  <%= ' Quienes somos |' %>  
  <%= ' |' %> 

  <%= ' Preguntas frecuentes |' %>    

    

  <%= ' Contactanos |' %> 

   <%= ' Mi Cuenta |' %>

Bienvenido <%= current_user&.username %>


<%= link_to t('.login'), new_user_session_path unless user_signed_in? || admin_signed_in?  %>
<%= link_to t('.register'), new_user_registration_path unless user_signed_in? || admin_signed_in? %>
<%= link_to t('.logout'), destroy_user_session_path(), data: {
  turbo_method: :delete,
  turbo_confirm: t('common.confirm')
}  if user_session %>
</div>