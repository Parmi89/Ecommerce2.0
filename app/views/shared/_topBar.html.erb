<div>
  <%= link_to t('.all_product'), products_path %>
  <%= ' Quienes somos |' %>  
  <%= link_to t('.frecuent_question'), frequentquestions_path %>    
  <%= ' Contactanos ' %> 
  <%= link_to t('.my_account'), edit_user_registration_path if user_signed_in?  %>
  
  <%= link_to t('common.log_in'), new_user_session_path unless user_signed_in? || admin_signed_in?  %>
  <%= link_to t('.register'), new_user_registration_path unless user_signed_in? || admin_signed_in? %>
  <%= link_to t('common.logout'), destroy_user_session_path(), data: {
    turbo_method: :delete,
    turbo_confirm: t('common.confirm')
  }  if user_session %> <%= current_user&.username %>
</div>